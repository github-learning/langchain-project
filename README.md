# tool-test

Small LangChain + OpenAI-compatible demos.


## Run

```bash
npm run hello
npm run tool:read
```

## Ask
LangChain JS 里 PromptTemplate.invoke 和 format 的区别（v1），并给官方文档/引用点
Runnable 的 pipe() 和 invoke() 使用方式（JS）


